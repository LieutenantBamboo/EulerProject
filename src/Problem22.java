import java.io.*;
import java.util.ArrayList;
import java.util.Scanner;

/**
 * Created by matthewconnorday on 15/03/16.
 */

public class Problem22 {
    /*
    String pathName = "/Users/matthewconnorday/Dropbox/Euler/names.txt";
    File file = new File(pathName);
    ArrayList<String> words = new ArrayList();
    Scanner scanner = new Scanner(file);                                    // Need to fix the file location problem

    public void call()
    {
        System.out.println(new File("names.txt").getAbsoluteFile());
        //read();
        //sort();
        System.out.println(calc());
    }
    /*
    public void read()
    {

        scanner.useDelimiter("\",\"");
        while(scanner.hasNext())
        {
            words.add(scanner.next());
        }

    }

    public void sort()
    {
        for(int x = 0; x < words.size(); x++)
        {
            for(int y = 0; y<words.size(); y++)
            {
                if(words.get(y).equals(words.get(y+1))){}                   // Not finished. Need a word comparing sub-routine
                else{break;}
            }
        }
    }
    public int calc()
    {
        int answer = 0;
        return answer;
    }
*/
}
