/**
 * Created by matthewconnorday on 28/06/16.
 */
public class Problem33 {

    public void call(){}

    public void curious()
    {
        int product = 0;

        for(int x = 0; x<100; x++){
            for(int y = 0; y < 100; y++){
                //if()
            }
        }

        System.out.println("The product of these four fractions is: "+ product);
    }


    public boolean similarChars(char[] first, char[] second)
    {
        boolean similar = false;

        for(int count = 0; count < Math.max(first.length,second.length); count++)
        {
            //if()
        }

        return similar;
    }

    public char[] toCharArray(int num)
    {
        String stringX = String.valueOf(num);
        char[] array = new char[stringX.length()];

        for(int count = 0; count < stringX.length(); count ++)
        {

        }

        return array;
    }
}
